openapi: 3.0.3
info:
  title: Shared schemas
  version: 1.0.0
paths: {}
components:
  schemas:
    Pet:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
          description: Pet identifier.
        name:
          type: string
          description: Pet name.
